//console.log('Hello World');

// Variables
let a = 10
const b = 20
var c = 30

//console.log(a, b, c);

// Fuonctions
function Add(a, b) {
    return a + b
}

const AddBis = function (a,b) {
    return a + b
}

const AddArrow = (a, b) => {
    return a + b
}

// tableaux
const tab = [1, 2, 3, 4, 5]

const tabDouble = tab.map(x => x * 2)

//console.log(tabDouble)
